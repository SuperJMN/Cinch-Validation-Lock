<Window x:Class="ComplexValidation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:complexValidation="clr-namespace:ComplexValidation"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <complexValidation:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <complexValidation:ListBoxEx ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" CanChangeSelection="{Binding CanSelectItem}" PromptCommand="{Binding PromptCommand}" />
        <CheckBox Grid.Row="1" Content="Hay cambios pendientes?" IsChecked="{Binding HasPendingChanges, Mode=TwoWay}" VerticalAlignment="Bottom" HorizontalAlignment="Right" />
    </Grid>
</Window>
